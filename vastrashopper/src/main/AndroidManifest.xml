<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.esolution.vastrashopper">

    <application
        android:allowBackup="false"
        android:label="@string/app_name"
        android:supportsRtl="true"
        android:theme="@style/Theme.Vastra.NoActionBar">
        <activity android:name=".ui.ShopperMainActivity" />
        <activity android:name=".ui.products.filters.ProductFilterActivity">
            <!--<intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>-->
        </activity>
        <activity android:name=".ui.products.ProductDetailsActivity"/>
    </application>

</manifest>