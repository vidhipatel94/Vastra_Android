<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.esolution.vastra">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.Vastra.NoActionBar">
        <activity android:name=".ui.SplashScreenActivity">

        </activity>
        <activity android:name=".ui.registration.StartupActivity" />
        <activity android:name=".ui.registration.LoginActivity" />
        <activity android:name=".ui.registration.RegisterUserTypeActivity" />
        <activity android:name=".ui.registration.RegistrationFormActivity" />
        <activity android:name=".ui.registration.EmailRegistrationActivity" />
        <activity android:name=".ui.registration.EmailVerificationActivity" />
    </application>

</manifest>